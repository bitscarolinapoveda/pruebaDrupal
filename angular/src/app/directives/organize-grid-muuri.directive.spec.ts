import { OrganizeGridMuuriDirective } from './organize-grid-muuri.directive';

describe('OrganizeGridMuuriDirective', () => {
  it('should create an instance', () => {
    const directive = new OrganizeGridMuuriDirective();
    expect(directive).toBeTruthy();
  });
});
